# macOS
.DS_Store

# Node
node_modules/
dist/
.vite/
coverage/
*.log
*.tsbuildinfo
vite.config.js
vite.config.d.ts

# Python
.venv/
.venv*/
venv/
env/
__pycache__/
*.py[cod]

# Env
.env
.env.local
.env.*.local

# Editor
.idea/
.vscode/*
!.vscode/tasks.json
!.vscode/extensions.json

# Claude / skills
.claude/
skills-main/

# Context / architecture docs (kept local)
context/

# Agent working notes (local only)
AGENTS.md
MEMORY.md

# Model training outputs (checkpoints are large; metrics are committed)
ml/outputs/

# Kaggle runner scratch (generated by ml/kaggle/runner.py)
ml/kaggle/.stage/
ml/kaggle/.stage_kernel/
ml/kaggle/.output/

# Transient per-run training scratch (regenerable; canonical results live in ml/reports/runs/*.json
# and the findings notebooks). Keeps the results commit lean.
ml/reports/history_*.csv
ml/reports/per_language_*.csv
ml/reports/encoder_summary.csv
ml/reports/perlang_summary.csv
ml/reports/perlang_lora_summary.csv

# Fine-tuned encoder checkpoints (~1.9 GB each). Too large for git -- GitHub rejects
# single files over 100 MB, and LFS free tier is 1 GB. Distribute via Kaggle/HF instead;
# ml/models/README.md documents how to obtain and load them.
ml/models/encoders/
