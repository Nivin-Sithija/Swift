\section{The Transfer Boundary Is Orthographic}
\label{sec:script}

\subsection{Where the advantage lives}

The pooled sentiment gain of $+0.0485$ is not spread evenly across the five
tracks (Table~\ref{tab:gain}). Every point of it is earned on text written in a
script the encoder was pretrained on. On the two romanized tracks the encoder
and a bag of character $n$-grams are indistinguishable.

\begin{table}[t]
\caption{LaBSE minus TF-IDF+SVM, sentiment \negf{} on test, paired bootstrap
within each track ($n=3079$ tickets per track). The last two rows are
differences of differences between paired tracks.}
\label{tab:gain}
\centering
\small
\begin{tabular}{llrr}
\toprule
Track & Script & Gain & $p$ \\
\midrule
english  & Latin (seen)         & $+0.0834$ & $<0.001$ \\
sinhala  & Sinhala (seen)       & $+0.0822$ & 0.004 \\
tamil    & Tamil (seen)         & $+0.0357$ & 0.144 \\
singlish & Latin (unseen form)  & $+0.0124$ & 0.712 \\
tamilish & Latin (unseen form)  & $+0.0225$ & 0.490 \\
\midrule
\multicolumn{2}{l}{sinhala $-$ singlish} & $\mathbf{+0.0698}$ & \textbf{0.012} \\
\multicolumn{2}{l}{tamil $-$ tamilish}   & $+0.0132$ & 0.762 \\
\bottomrule
\end{tabular}
\end{table}

The decisive row is the second from last. The Sinhala and Singlish tracks hold
the same tickets, in the same language, with the same labels and identifiers,
differing only in whether the text is written in the Sinhala abugida or
transliterated into Latin characters. A difference in gain between them cannot
be a language, topic, or sampling effect. Tested directly it is $+0.0698$ with a
95\% interval of $[0.014, 0.128]$, $p=0.012$. Fig.~\ref{fig:gain} shows the same
decomposition with intervals.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{encoder_gain_by_script.pdf}
\caption{Encoder gain over the classical champion by track, 95\% bootstrap
intervals. The gain is significant only where the script was in pretraining.}
\label{fig:gain}
\end{figure}

The Tamil pair does not replicate ($p=0.762$) and we do not claim it does.
Section~\ref{sec:corpus} gives the reason to expect it will not: the two pairs
differ in their own English content, 64.6\% loanword retention against 7.6\%, so
they do not share a baseline. The finding holds for Sinhala and is open for
Tamil.

\subsection{The same shape between two encoders}

If the boundary is orthographic it should appear wherever two systems differ in
script coverage, not only between an encoder and $n$-grams. LaBSE and mmBERT are
tied on intent development ($p=0.944$) and separate on test by $+0.0155$
\macrof{}. That gap is confined to the two non-Latin scripts: sinhala $+0.0196$
($p=1.8\times10^{-6}$) and tamil $+0.0178$ ($p=2.8\times10^{-5}$), against
english $+0.0037$ and singlish $+0.0022$, both non-significant. English and
Singlish are both Latin and both show nothing. The result survives dropping the
defective Tanglish track ($+0.0106$, $p=7.1\times10^{-7}$).

\subsection{The mechanism is vocabulary coverage}

Tokenizer studies usually report subword fertility, the pieces produced per
word, on the reasoning that a tokenizer shattering a language will encode it
poorly. On our data fertility predicts nothing and out-of-vocabulary character
rate predicts the deficit.

MuRIL is the clearest case. Its tokenizer maps 64.5\% of Sinhala characters to
\texttt{[UNK]} and 0.0\% of every other track, including both romanized ones. Its
Sinhala sentiment score falls 23.4 points below its own English score, while its
Sinhala fertility of 1.18 is the \emph{lowest} of any model--track pair we
measured. It is not fragmenting Sinhala. It is deleting it.

Across the twenty non-English cells, fertility is unrelated to the deficit
(Spearman $\rho=-0.035$). The \texttt{[UNK]} rate reaches $\rho=-0.521$, but we
report that as a case study rather than a fitted relationship, because only two
of the twenty cells have a non-zero \texttt{[UNK]} rate at all: the coefficient
is carried by those cells, not by a range. What the data support is the
contrast. Cells whose tokenizer discards more than 10\% of characters lose 0.099
more than cells whose tokenizer does not, and every cell in the roster that
holds its script intact behaves the same way regardless of how finely it splits
the text. Establishing this as a relationship needs more high-\texttt{[UNK]}
tokenizers than the current roster contains.

This explains why an Indic-targeted encoder underperforms a general multilingual
one on Sri Lankan text. MuRIL covers Indian languages and the Sinhala Unicode
block is not among them. The model does not transfer poorly to Sinhala. It never
receives Sinhala.

The hole recurs on a second task and shrinks with the label space. MuRIL scores
0.8072 \macrof{} on native Sinhala priority against 0.9095 on English, a
10.2-point deficit on the same tickets whose intent deficit is 20.0 points. The
input destruction is identical; a three-way task tolerates roughly twice as much
of it as a 77-way one. A coarse triage tier can survive a tokenizer that a
fine-grained intent router cannot.
